import PickerComponent from './tpl.vue'

const Picker = {
    install: function( Vue, options ) {

        Vue.prototype.$picker = function (options) {
            
        }

    },

}

export default Picker